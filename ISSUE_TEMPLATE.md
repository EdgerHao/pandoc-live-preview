## 功能请求：为 Pandoc 交叉引用插件添加公式（Equation）支持

### 功能描述

希望 Pandoc Live Preview 插件能够支持公式（equation）的 Pandoc 交叉引用功能，与现有的图片（figure）和表格（table）类似。

### 动机

在学术写作中，公式和图片、表格一样重要，都需要交叉引用功能。目前插件只支持：
- `{#fig:xxx}` 和 `@fig:xxx`（图片）
- `{#tbl:xxx}` 和 `@tbl:xxx`（表格）

缺少公式支持导致在编写包含大量数学公式的学术文档时，无法获得完整的实时预览体验。

### 建议的实现方式

#### 1. 公式定义和引用语法
```markdown
$$
E = mc^2
$$ {#eq:mass-energy}

如公式 @eq:mass-energy 所示...
```

#### 2. 渲染效果
- 定义处（`{#eq:xxx}`）: 显示为 **式1**（带虚线边框）
- 引用处（`@eq:xxx`）: 显示为 **式1**（带实线边框）

#### 3. 配色方案建议
- 使用紫色系（与蓝色图片、橙色表格形成对比）
- 定义处：深紫色 + 紫色虚线边框
- 引用处：标准紫色 + 浅紫实线边框

#### 4. 配置选项
- 添加"公式前缀"设置项（默认"式"）
- 支持自定义编号格式

#### 5. 快捷功能
- 添加"插入公式ID"命令
- 在自动补全建议中包含公式类型

### 适用场景

- 学术论文写作（物理、数学、经济学等）
- 包含大量数学公式的技术文档
- 任何使用 Pandoc 进行学术写作的用户

### 优先级

**中等优先级** - 这是一个常见需求，可以显著提升学术写作体验。

---

**标签**: enhancement, feature-request, pandoc, cross-reference

**相关功能**: 
- 图片交叉引用 `{#fig:}` 和 `@fig:`
- 表格交叉引用 `{#tbl:}` 和 `@tbl:`
